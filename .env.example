PORT=8080
PUBLIC_BASE_URL=http://127.0.0.1:8080

# Optional: fill these to create real payOS payment links.
# Without them, the app uses the built-in mock checkout for testing.
PAYOS_CLIENT_ID=
PAYOS_API_KEY=
PAYOS_CHECKSUM_KEY=
